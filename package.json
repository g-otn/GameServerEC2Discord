{
  "scripts": {
    "setup-discord-app": "node --env-file=.env ./scripts/add-slash-commands"
  },
  "workspaces": [
    "./base_region/lambda/handle-interaction",
    "./base_region/lambda/manage-instance"
  ]
}
